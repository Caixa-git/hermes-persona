# Contributing

PRs welcome. This repo distributes a single prompt file (`skills/persona/SKILL.md`).

**To contribute:**
1. Branch from `develop`
2. Edit `skills/persona/SKILL.md`
3. Run `python3 test_benchmark.py` — all tests must pass
4. PR to `develop`

See `archive/references/` for historical design docs and experiment data.
